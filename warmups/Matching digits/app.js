function sameDigits(numA, numB){
    if (numA % 10) === (numB % 10){
        console.log ("true")
    } else {
        console.log ("false")
    }
    
    
}
        
//write a function that takes two numbers and returns true if they share a digit
        //use the mod to determine the remainder of each num and see if they equal eachother

function arrayPrint(arr){
    for (var i = 0; i < arr.length;i++){
        console.log([i]);
    }
    
};

var example=[1,2,3,4,5,6,7,8]

var exampleStrings=["Curty", "Dirty", "Flirty", "Churty", "Paturdy"]

